@model IEnumerable<ELECTION.Models.BureauDeVote>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Emplacement)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Responsable)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Region)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Emplacement)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Responsable)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Region.NomRegion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BureauID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BureauID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BureauID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
